pipeline {

    agent any

    tools {
        docker 'docker'
    }

    stages {

        stage("list") {
            steps {
                sh 'ls'
                sh 'docker --version'
            }
        }

        stage("build") {
            steps {
                sh 'docker image ls'
            }
        }

    }

}