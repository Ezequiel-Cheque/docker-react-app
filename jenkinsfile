pipeline {

    agent any


    stages {

        stage("build") {
            steps {
                sh 'docker build -t heroes-app .'
            }
        }
        
        stage("review") {
            steps {
                sh 'docker image ls'
            }
        }

    }

}