pipeline {

    agent {
        dockerfile {
            filename 'Dockerfile.build'
            label 'my-defined-label'
            additionalBuildArgs  '--build-arg version=1.0.2'
        }
    }

    stages {
        stage("test") {
            steps {
                sh 'docker images'
            }
        }
    }

}